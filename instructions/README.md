# Instructions

Operational runbooks that are easier to keep separate from the main product README live here.

Current entries:

- [`docker-desktop-private-registry.md`](docker-desktop-private-registry.md) — how to make Docker Desktop on macOS trust an internal registry certificate chain
- [`colima-private-registry.md`](colima-private-registry.md) — how to make Colima trust an internal registry certificate chain, plus what to do when guest DNS does not follow macOS scoped VPN DNS
