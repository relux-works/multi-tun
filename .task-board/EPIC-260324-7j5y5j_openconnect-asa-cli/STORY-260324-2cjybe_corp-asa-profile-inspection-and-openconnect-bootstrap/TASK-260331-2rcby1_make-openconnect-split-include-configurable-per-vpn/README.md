# TASK-260331-2rcby1: make-openconnect-split-include-configurable-per-vpn

## Description
Add per-profile/per-server split-include config overrides so Corp domain masks, routes, and nameservers are selected from config instead of one global block. This is needed to avoid hard-wiring corp.example behavior across different VPNs and to let profiles decide whether hosts like portal.corp.example should stay public or go into VPN.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
