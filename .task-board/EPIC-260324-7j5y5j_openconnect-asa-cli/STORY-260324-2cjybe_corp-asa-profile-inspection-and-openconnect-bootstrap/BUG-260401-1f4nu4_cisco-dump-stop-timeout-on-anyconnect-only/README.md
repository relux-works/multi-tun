# BUG-260401-1f4nu4: cisco-dump stop timeout on AnyConnect only

## Description
cisco-dump stop times out in at least the AnyConnect-only capture path even though traffic capture succeeds and the target site stays reachable. Investigate helper/tcpdump shutdown sequencing and final probe collection.

## Scope
(define bug scope / affected area)

## Acceptance Criteria
(define fix acceptance criteria)
