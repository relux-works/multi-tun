# TASK-260402-1z0hj7: implement agent-facing openconnect setup flow

## Description
Implement an agent-facing openconnect-tun setup flow. The user should be able to say "set up openconnect", the agent asks for the VPN name, runs openconnect-tun setup, seeds the default config in full mode with no bypasses, writes placeholder auth entries into Keychain/config, and returns the resulting config path for inspection.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
