# TASK-260402-2wvy0n: refresh-openconnect-readme-config-reference

## Description
Refresh the openconnect-tun README configuration reference after the config remaster lands. Rewrite the section around the new default.server_url plus default.profile root selection and the nested servers.<url>.profiles.<profile>.mode plus split_include layout. Remove outdated wording about the old top-level servers versus profiles split, explicitly document vpn_domains as suffix masks, and keep the openconnect docs focused on the final live config model.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
