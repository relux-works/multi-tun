# TASK-260326-265vlw: cisco-anyconnect-dumper-and-start-aliases

## Description
Add a cisco-dump companion CLI with start/status/stop session semantics, runtime/session logs under ~/.cache/cisco-dump, and initial AnyConnect capture hooks (localhost IPC tcpdump, Cisco log snapshots, process snapshots). Also add start as a compatibility alias for vless-tun and openconnect-tun so command semantics line up across tools.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
