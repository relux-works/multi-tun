# BUG-260401-dpg2o2: legacy launchd session cleanup

## Description
Teach vless-tun stop/status to detect and control a live legacy works.relux.vless-tun LaunchDaemon even when runtime/current-session.json is missing after reboot.

## Scope
(define bug scope / affected area)

## Acceptance Criteria
(define fix acceptance criteria)
