# TASK-260402-z27mpv: implement agent-facing vless setup flow

## Description
Implement an agent-facing vless-tun setup flow. The user should be able to say "set up vless", the agent drives vless-tun setup, scaffolds the default config, and returns the resulting config path for inspection. Unlike openconnect, this flow currently should not depend on Keychain placeholder writes.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
