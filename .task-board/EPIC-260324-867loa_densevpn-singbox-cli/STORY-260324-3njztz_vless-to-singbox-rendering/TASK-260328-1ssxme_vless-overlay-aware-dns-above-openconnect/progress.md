## Status
development

## Assigned To
codex

## Created
2026-03-28T19:49:26Z

## Last Update
2026-04-02T11:04:57Z

## Blocked By
- (none)

## Blocks
- (none)

## Checklist
(empty)

## Notes
2026-03-28 live repro: standalone openconnect split-include now preserves primary DNS, but starting vless-tun above active openconnect still breaks non-Corp lookups. sing-box log for session 20260328T194341Z shows dns/local[dns-direct] querying DHCP DNS on en0 ([192.168.1.1:53], search [IGD_MGTS]) instead of consuming openconnect supplemental resolver files. openconnect session 20260328T194252Z confirms Corp /etc/resolver/* files are present and global resolver state is clean. Goal: make vless render overlay-aware so generic DNS uses proxy path while Corp/corp suffixes use openconnect-specific resolvers.
2026-03-28 implementation update: added overlay-aware vless render/start behavior. When openconnect has an active session, cli resolves overlay DNS metadata from its runtime session file and passes domains + nameservers into sing-box rendering. Under overlay mode, generic bypass DNS no longer routes to dns-direct/local DHCP; direct outbound switches to dns-proxy for generic direct traffic, while Corp/corporate suffixes get a dedicated dns-overlay UDP server bound to openconnect nameservers. Added coverage in internal/openconnect/session_test.go and internal/singbox/render_test.go. go test ./... passed and vless-tun was rebuilt. Live smoke still pending.
2026-03-29 follow-up: successful overlay config for session 20260328T221706Z already showed dns-overlay and direct.domain_resolver=dns-proxy, but sing-box still initialized dns/local[dns-direct] and logged DHCP resolver discovery on en0. Per sing-box local DNS behavior on macOS, that path still uses DHCP/NetworkExtension, so overlay mode now removes dns-direct/local entirely. Overlay config keeps only dns-proxy for generic lookups and dns-overlay UDP to Corp nameservers for corporate suffixes. go test ./... passed and vless-tun rebuilt for next live smoke.
2026-03-29 live behavior after overlay config changes: standalone openconnect split-include remains healthy, and standalone vless remains healthy. When both tunnels are up together, external DNS still collapses while Corp/internal hosts keep resolving; stopping openconnect immediately restores external resolution for the already-running vless session without restarting vless. This points to remaining system DNS ownership conflict during overlay rather than route blackholing or broken standalone configs.
2026-03-29 deeper live review: overlay session 20260328T223428Z no longer logs dns/local[dns-direct] at all, so the latest overlay render path was active. The session log still shows successful outbound/vless[proxy] and outbound/direct[direct] traffic to public and corp IPs while openconnect was up. Generated standalone config inspected later is not representative of that overlay run. Remaining failure therefore looks like macOS/system DNS ownership conflict while both tunnels are alive, not a stale render path or a dead sing-box process.
2026-03-29 implementation update: vless overlay sessions now apply a macOS primary DNS handoff on the active default network service instead of relying only on sing-box DNS interception. On overlay start, session startup captures the current service + DNS server state via route/networksetup, switches that service to the tun IPv4 address, persists restore metadata in the session file, and restores the previous DNS state on stop, kill, or stale cleanup. Added parser tests for route/networksetup output plus handoff/restore coverage in internal/session/dns_handoff_test.go. go test ./... passed and vless-tun was rebuilt for the next live smoke.
2026-03-30 live finding: the new vless DNS handoff is active in session 20260330T145636Z (dns_handoff_apply_begin/ok for Wi-Fi -> 172.19.0.1), and that aligns with the regression where openconnect->vless now dies immediately after second tun start. Separate finding: reverse order still fails too. In session 20260330T153157Z, vless was standalone (dns/local[dns-direct] on en0), then openconnect connected on top; after that non-Corp traffic still died while Corp domains survived. That means there are two issues now: the new handoff path regressed openconnect->vless, and openconnect-over-existing-tun remains broken independently of the handoff path.
2026-03-30 mitigation: the macOS primary DNS handoff for vless overlay is now disabled by default and kept only behind VLESS_TUN_ENABLE_DNS_HANDOFF=1. Live logs showed that the handoff itself regressed the openconnect->vless path by forcing Wi-Fi DNS to the tun IPv4 address (172.19.0.1). That experiment should not stay enabled while the deeper coexistence bug is still unresolved.
2026-03-30 readiness fix: live log openconnect-session-20260330T161043Z showed CLI emitting connected before the connect-phase wrapper finished applying split-include state. In that log, vpnc_wrapper_base_exit: 0 and vpnc_wrapper_snapshot: after from pre-init appeared before the later connect-phase markers vpnc_wrapper_route_override, vpnc_wrapper_dns_shim: apply split-include, and probe route sync. openconnect start now performs a second post-connect convergence wait after PID stabilization and before reporting phase: connected. The wait watches the wrapper log for the last connect/reconnect event to reach route override + DNS shim + probe-sync completion (when expected), so follow-up commands do not race ahead of split-include route/DNS installation. Added parser coverage in internal/openconnect/session_test.go; go test ./... passed and openconnect-tun rebuilt.
2026-03-30 follow-up: after the first readiness fix, fresh live session openconnect-session-20260330T202007Z still showed two concrete residual issues. First, split-include probe sync for gitlab.services.corp.example still ran only once and often logged vpnc_wrapper_probe_route_sync_resolve_failed during connect, which matches the user report that Corp resources were still unavailable for a few seconds after phase: connected. Second, after openconnect stop the system DNS still showed resolver #8 domain search.tailscale with Corp search domains in scutil --dns, proving split-include was leaving stale search-domain pollution even though primary DNS ownership stayed on en0. Fix: split-include now sanitizes search.tailscale too, probe-route sync retries until resolution succeeds, and post-connect readiness requires successful apply probe sync instead of treating resolve_failed as converged. go test ./... passed and openconnect-tun rebuilt.
2026-03-30 probe refinement: the previous hard readiness gate was timing out for the wrong reason. In failed session openconnect-session-20260330T203522Z, route overrides and DNS shim had already landed, search.tailscale was restored+sanitized, and the only missing piece was the expensive apply probe still sitting at vpnc_wrapper_probe_route_sync_retry remaining=11. The bottleneck was the probe resolution path itself, which still spent too long in system resolver lookups before the gate timeout killed the session. Refined the apply probe to prefer fast direct dig queries against the Corp nameservers, keep system lookup only as fallback, reduce retry count cost, and extend post-connect timeout to 20s. go test ./... passed and openconnect-tun rebuilt.
2026-03-30 apply-probe cleanup: the repeated timeout on openconnect-session-20260330T203936Z exposed one more hidden bug in the readiness path. Even after switching apply probes toward direct Corp DNS, sync_probe_host_routes was still calling the retry helper with 12 attempts, and the apply resolver still carried the old expensive fallback path. That is why the log still stalled at vpnc_wrapper_probe_route_sync_retry remaining=11 and never had a realistic chance to finish within the 20s gate. Cleanup: apply probes now use the fast Corp-DNS-first resolver only, the slow generic fallback is removed from the apply path, and the retry count used by connect-phase probe sync is reduced to 6. go test ./... passed and openconnect-tun rebuilt.
2026-03-30 async warmup pivot: failed session openconnect-session-20260330T204240Z proved the hard probe gate was conceptually wrong for the connect hook. Route overrides, DNS shim, and search.tailscale sanitation all completed, but while the wrapper was still doing probe retries the underlying openconnect/vpn-slice hook got interrupted (`Failed to spawn script ... Interrupted system call`) and the session aborted. That means hostname warmup cannot stay inside the blocking connect hook. Fix: connect convergence now gates only on route/DNS installation; gitlab.services.corp.example host-route warmup is launched asynchronously from the wrapper and observed via a separate best-effort wait on the CLI side. go test ./... passed and openconnect-tun rebuilt.
2026-03-30 coexistence follow-up: live logs show vless proxy traffic survives during the first 30-40s with openconnect also up, so the remaining bug is not total proxy failure. Added route_exclude_address to overlay tun render from openconnect split-include route overrides (corp CIDRs + Corp DNS /32) so vless stops intercepting private/control traffic owned by openconnect.
2026-03-31 live smoke after route_exclude_address patch: with openconnect session 20260331T081745Z and vless session 20260331T081831Z both active, terminal-level network paths are healthy. dig github.com and avito.ru resolve via 1.1.1.1 on utun10, curl https://github.com returns 200, curl https://gitlab.services.corp.example returns 302, git ls-remote to git@gitlab.services.corp.example succeeds, and a fresh shallow clone of ios-membrana-app completed in ~/Downloads/multi-tun-tests/2026-03-31/ios-membrana-app-smoke at main@6d4cd23. Remaining discrepancy is therefore not a global terminal/data-plane outage; it is likely app/browser-specific or tied to how macOS surfaces resolver/search-domain state while both utun9 and utun10 are present.
2026-03-31 overlay DNS handoff revised: the old tun-IP handoff was replaced with system DNS handoff to the configured external vless DNS servers instead of 172.19.0.1. Overlay sessions now, by default, set the active network service DNS to the vless proxy DNS address list (currently derived from render.proxy_dns.address) and restore the prior DNS configuration on stop. Opt-out only: VLESS_TUN_ENABLE_DNS_HANDOFF=0.
2026-03-31 saved live manual-vs-app baseline snapshot to /tmp/multi-tun-baselines-2026-03-31.md for later comparison.
2026-03-31: manual AnyConnect+v2raytun baseline showed the remaining delta was resolver ownership, not just route excludes. The working manual stack publishes primary DNS on the utun interface, while vless-tun was only overriding Wi-Fi DNS servers via networksetup. Reworked vless overlay handoff to publish a synthetic scutil State:/Network/Service/<id> DNS+IPv4 record on the sing-box utun using vpn-core, with overlay domains as SearchDomains and proxy DNS servers as ServerAddresses. Networksetup DNS override remains only as fallback if scutil handoff cannot be applied. Added session metadata/status output for scutil handoff and unit coverage around apply/restore commands. go test ./... is green. Live retest still pending.
2026-03-31 startup readiness follow-up: live retest showed the remaining coexistence issue had narrowed to a startup race. With openconnect+tun overlay active, browser / Apple Music / git traffic could already work while curl still intermittently timed out resolving public domains immediately after vless-tun start. Implemented a post-start readiness gate in internal/session/session.go after dns_handoff_apply_ok: overlay tun starts on macOS now wait for scutil handoff visibility plus consecutive successful public system-resolver lookups (github.com + yandex.ru) before reporting start success. On timeout, start fails and tears the session down instead of returning early. Added coverage in internal/session/session_test.go, updated README.md, ran go test ./..., and rebuilt vless-tun.
2026-04-01 user live matrix refined the remaining coexistence bug. Order now matters concretely: when vless starts first and openconnect comes up on top, Corp domains resolve but unrelated external domains fail to resolve; when openconnect starts first and vless starts second, general resolution works again (except the separate portal case). This narrows the overlay issue to startup-order-sensitive DNS ownership / resolver merge behavior rather than a universal two-tunnel data-plane failure.
2026-04-02 coexistence follow-up: live host had vless-tun active with broad bypasses (.ru/.рф), but openconnect bootstrap to vpn-gw2.corp.example still got trapped by overlay DNS because sing-box overlay mode routes matching corporate suffixes to dns-overlay before evaluating generic bypass suffixes. Fix in progress: make bypass DNS rules precede overlay DNS rules so explicit/public bypass wins over broader overlay domain routing.
2026-04-02 fix: internal/singbox overlay render now gives bypass DNS higher priority than broader overlay domain routing, but only in tun mode. When bypass suffixes exist, render adds dns-direct under overlay and orders DNS rules as proxy-exceptions -> ru-direct -> dns-overlay -> final proxy, so vpn-gw2.corp.example and other explicit/public bypasses stop getting trapped by corp.example overlay DNS during openconnect bootstrap. Verified with go test ./... and rebuilt ./vless-tun.

## Precondition Resources
(none)

## Outcome Resources
(none)
