# TASK-260402-2d9b15: setup-global-vpn-config-skill-package

## Description
Create the global vpn-config skill packaging/setup flow: verify required tools including TOTP auth prerequisites, install missing tools, install the skill into the global runtime, fan out repo-local symlinks from that global install, and remove stale resource/degitization leftovers so globals keep only the working runtime schema.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
